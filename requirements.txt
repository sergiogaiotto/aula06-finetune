# Apenas para uso fora do Colab (Codespaces, máquina local, scripts standalone)
# Dentro do Colab, o notebook instala suas próprias dependências.

openai>=1.50.0
pandas>=2.2.0
pyyaml>=6.0.1
python-dotenv>=1.0.1
tabulate>=0.9.0
tqdm>=4.66.0
